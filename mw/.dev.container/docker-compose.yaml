version: '3'
services:
  mw:
    build:
      context: ../
      dockerfile: Dockerfile
    ports:
      - 4000:4000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:z
    env_file:
      - .config/.env
    privileged: true

